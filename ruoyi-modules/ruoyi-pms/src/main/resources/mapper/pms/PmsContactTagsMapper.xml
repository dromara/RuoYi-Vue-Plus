<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.dromara.pms.mapper.PmsContactTagsMapper">

    <!-- 根据分类查询标签列表 -->
    <select id="selectTagsByCategory" parameterType="String" resultType="org.dromara.pms.domain.vo.PmsContactTagsVo">
        SELECT
            tag_id,
            dept_id,
            name,
            color,
            category,
            description,
            is_system,
            sort_order
        FROM pms_contact_tags
        WHERE del_flag = '0'
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        ORDER BY sort_order ASC, tag_id ASC
    </select>

    <!-- 查询可用的标签分类列表 -->
    <select id="selectDistinctCategories" resultType="String">
        SELECT DISTINCT category
        FROM pms_contact_tags
        WHERE del_flag = '0'
        AND category IS NOT NULL
        AND category != ''
        ORDER BY category ASC
    </select>

    <!-- 根据部门ID查询标签列表 -->
    <select id="selectTagsByDeptId" parameterType="Long" resultType="org.dromara.pms.domain.vo.PmsContactTagsVo">
        SELECT
            tag_id,
            dept_id,
            name,
            color,
            category,
            description,
            is_system,
            sort_order
        FROM pms_contact_tags
        WHERE del_flag = '0'
        AND (dept_id = #{deptId} OR dept_id IS NULL)
        ORDER BY sort_order ASC, tag_id ASC
    </select>

</mapper>
